CREATE TABLE ubicacion(
    id varchar(10) NOT NULL PRIMARY KEY,
    pais varchar(50) NOT NULL,
    ciudad varchar(50) NOT NULL,
    ciudad_abreviada varchar(10) NOT NULL,
    zona_horaria varchar(20) NOT NULL
);